spring:
  liquibase:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:tc:mysql:////boat-db
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver